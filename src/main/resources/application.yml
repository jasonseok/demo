server:
  port: 8088
  context-path: /

security:
  user:
    name: user
    password: test
  oauth2:
    client:
      client-id: foo
      client-secret: bar
    authorization:
      token-key-access: isAuthenticated()